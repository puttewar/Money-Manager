
<table class="table table-striped table-hover ">
  <thead>
	  <tr class="info">
		  <th> Category Name </th>
		  <th> Description </th>
          <th> Show </th>
		  <th> Edit </th>
		  <th> Delete </th>
		  <th></th>
	  </tr>
  </thead>
  <tbody>
    <% @income_category.each do |e| %>
      <tr>
        <td> <%= e.income_source %> </td>
        <td> <%= e.description %> </td>
        <td> <%= link_to "show",income_category_show_path(e) %></td>
        <td> <%= link_to "Edit",income_category_edit_path(e) %></td>
        <td> <%= link_to "Delete",income_category_destroy_path(e), method: :delete, data: { confirm:'are u sure?' } %></td>
        <td></td>
      </tr>
    <% end %>
  </tbody>
</table>